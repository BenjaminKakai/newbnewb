// app/settings/page.tsx

import Settings from "@/features/settings/SettingsPage"

export default function SettingsPage() {
  return <Settings />
}

export const metadata = {
  title: 'Settings - WasaaChat',
  description: 'Settings page for WasaaChat',
}